import turtle
pat = turtle.Turtle()
for i in range(20):
    for i in range(4):
        pat.forward(100)
        pat.color('green')
        pat.right(38)
        pat.forward(100)
        pat.color('orange')
        pat.right(40)
    pat.right(36)
    pat.color('red')
    pat.forward(60)
    pat.right(42)
    pat.forward(100)
    
